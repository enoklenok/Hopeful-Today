import TopInfoList from '@components/TopInfoList';

const TopInfo = () => {
  return (
    <section className="flex flex-col flex-[2] items-end">
      <TopInfoList />
    </section>
  );
};

export default TopInfo;
