import { TopInfo, Character, BottomInfo } from '@components';

const Main = () => {
  return (
    <main className="px-4 py-3">
      <TopInfo />
      <Character />
      <BottomInfo />
    </main>
  );
};

export default Main;
