// * Layouts
import { Background, Header, Main } from '@layouts';

const Layout = () => {
  return (
    <Background>
      <Header />
      <Main />
    </Background>
  );
};

export default Layout;
